<div class="container">
  <div class="menu">
    {{#link-to "index"}}
      <h1>
        <em>My Portfolio</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to "about" class="menu-about"}}
        About
      {{/link-to}}
      {{#link-to "contact" class="menu-contact"}}
        Contact
      {{/link-to}}
      {{#link-to "projects" class="menu-projects"}}
        Projects
      {{/link-to}}
    </div>
    <div class="right coffeetron"></div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>
